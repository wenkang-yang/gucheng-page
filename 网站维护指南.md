# 孤城网站维护指南

这份文档用于维护个人网站 [gucheng.ink](https://gucheng.ink)。

项目由四部分组成：

- GitHub：保存源代码和历史版本。
- Astro：把 Markdown、JSON、图片和页面组件构建成静态网站。
- Vercel：监听 GitHub 的 `main` 分支，并自动构建、发布网站。
- 阿里云：注册 `gucheng.ink`，并负责 DNS 解析。

日常更新的基本流程是：

```text
修改内容 → 本地预览 → 检查与构建 → Git 提交 → 推送 GitHub → Vercel 自动发布
```

## 一、常用文件位置

| 要修改的内容 | 文件或文件夹 |
| --- | --- |
| 首页介绍 | `src/content/home/index.md` |
| 首页随机诗句 | `src/data/quotes.ts` |
| Writing | `src/content/writing/` |
| Notes | `src/content/notes/` |
| Photos | `src/content/photos/` |
| Projects | `src/content/projects/data.json` |
| 网站名称、域名、导航等 | `src/config.ts` |

各内容文件夹中也有简短说明：

- `src/content/writing/_README.md`
- `src/content/notes/_README.md`
- `src/content/photos/README.md`
- `src/content/projects/README.md`

## 二、添加 Writing

在 `src/content/writing/` 中新建一个 Markdown 文件。

文件名建议使用小写英文和连字符，例如：

```text
my-new-essay.md
```

基本格式：

```md
---
title: 新文章的标题
description: 用一句话介绍这篇文章
pubDate: 2026-08-07
tags:
  - 随笔
---

这里开始写正文。
```

发布后的网址为：

```text
https://gucheng.ink/writing/my-new-essay/
```

注意事项：

- 开头和结尾的 `---` 不能删除。
- `title`、`pubDate` 是重要字段。
- 日期统一写成 `年-月-日`。
- 文件名不要使用空格，尽量不用中文。
- 暂时不想发布时，可以在头部加入 `draft: true`。
- 准备发布时删除 `draft: true`，或者改为 `draft: false`。

## 三、添加 Notes

在 `src/content/notes/` 中新建 Markdown 文件，格式与 Writing 相同。

例如：

```md
---
title: 某门课程笔记
description: 课程内容与复习要点整理
pubDate: 2026-08-07
tags:
  - 课程笔记
---

## 第一章

从这里开始写笔记。
```

假设文件名为 `course-notes.md`，网址就是：

```text
https://gucheng.ink/notes/course-notes/
```

## 四、添加 Photos

照片需要完成两步：放入图片文件，并在 `data.json` 中登记。

### 1. 放入图片

可以按类别建立子文件夹，例如：

```text
src/content/photos/campus/spring-campus.jpg
src/content/photos/daily/evening-road.webp
```

建议：

- 文件名使用小写英文和连字符。
- 优先使用 JPG 或 WebP。
- 上传前适当压缩，避免单张照片过大。
- 发布包含他人的清晰照片前，注意隐私和授权。

### 2. 登记照片

编辑 `src/content/photos/data.json`：

```json
[
  {
    "id": "campus/spring-campus",
    "desc": "春天的校园。"
  },
  {
    "id": "daily/evening-road",
    "desc": "傍晚路过时随手拍下。"
  }
]
```

注意事项：

- `id` 不写 `.jpg`、`.png` 或 `.webp` 扩展名。
- `id` 必须与图片的文件夹和文件名一致。
- 多个对象之间需要英文逗号。
- `desc` 是页面文案，也是图片的辅助说明。
- 调整数组中的顺序，就会调整页面中的展示顺序。

发布照片时，应同时提交图片和 `data.json`：

```powershell
git add -- src/content/photos/campus/spring-campus.jpg src/content/photos/data.json
```

## 五、添加 Projects

编辑 `src/content/projects/data.json`。

示例：

```json
[
  {
    "id": "项目名称",
    "link": "https://github.com/wenkang-yang/项目仓库",
    "desc": "用一句话介绍这个项目",
    "icon": "i-uil-github-alt",
    "category": "个人项目"
  }
]
```

注意事项：

- `link` 必须是完整网址。
- 多个项目之间需要英文逗号。
- 如果还没有项目，保持文件内容为 `[]` 即可。

## 六、修改首页

### 修改个人介绍

编辑：

```text
src/content/home/index.md
```

这里使用普通 Markdown，可以直接修改段落。

### 添加随机诗句

编辑：

```text
src/data/quotes.ts
```

在 `quotes` 数组中增加：

```ts
{
  text: '你想展示的句子。',
  source: '作者或出处',
},
```

如果没有出处，可以省略 `source`：

```ts
{
  text: '你想展示的句子。',
},
```

每个对象结尾的英文逗号不要漏掉。引用名言、诗句时应核实原文和出处。

## 七、本地预览

在项目终端中进入项目目录：

```powershell
cd D:\Projects\wenkang-site
```

后台启动开发服务器：

```powershell
npx.cmd astro dev --background
```

然后访问：

```text
http://127.0.0.1:4321
```

管理后台开发服务器：

```powershell
npx.cmd astro dev status
npx.cmd astro dev logs
npx.cmd astro dev stop
```

本地预览只在自己的电脑上可见，不会直接修改正式网站。

## 八、发布前检查

至少运行以下两项：

```powershell
npm.cmd run check
npm.cmd run build
```

- `check` 检查 Astro、TypeScript 和内容格式。
- `build` 模拟 Vercel 的正式构建过程。

如果修改了组件、样式或其他代码，还可以运行：

```powershell
npm.cmd run lint
npm.cmd run format
```

看到错误时先不要推送，优先根据错误中显示的文件名和行号处理。

## 九、使用 Git 发布

### 1. 查看改动

```powershell
git status
git diff
```

### 2. 只暂存准备发布的文件

例如发布一篇文章：

```powershell
git add -- src/content/writing/my-new-essay.md
```

不建议习惯性使用 `git add .`，否则可能把未完成或无关的内容一起发布。

### 3. 创建一次版本记录

```powershell
git commit -m "Add new essay"
```

提交说明可以使用英文或中文，但应简短说明这次做了什么。

### 4. 推送到 GitHub

```powershell
git push origin main
```

Vercel 检测到 `main` 分支变化后会自动部署。通常等待一两分钟，再访问：

```text
https://gucheng.ink
```

如果浏览器仍显示旧内容，可以强制刷新，或者稍等片刻后重新打开。

## 十、直接在 GitHub 网页修改

少量文字修改也可以直接在 GitHub 仓库网页完成：

1. 打开 `wenkang-yang/gucheng-page`。
2. 找到需要编辑的文件。
3. 点击铅笔按钮编辑。
4. 填写提交说明并提交到 `main`。
5. 等待 Vercel 自动部署。

新增文章时可以使用 **Add file → Create new file**。上传照片时可以使用
**Add file → Upload files**。

GitHub 网页无法像本地一样方便地提前运行 `check` 和 `build`，因此较大的修改仍建议在本地完成。

## 十一、域名和账号维护

### 域名

- `gucheng.ink` 在阿里云注册，需要按年续费。
- 建议开启到期提醒，并提前检查续费价格。
- 当前根域名使用 A 记录连接 Vercel。
- 不要随意修改 DNS 服务器或删除解析记录。
- 如果 Vercel Domains 页面将来明确要求更换记录值，以 Vercel 当时显示的值为准。

### HTTPS

HTTPS 证书由 Vercel 自动签发和续期，无需额外购买阿里云 SSL 证书。

### 备案

当前网站部署在 Vercel，不使用中国内地服务器，因此现阶段不办理 ICP 备案。如果以后迁移到中国内地服务器，应先按照实际接入商要求办理备案。

### 账号安全

- GitHub、Vercel 和阿里云都应开启两步验证。
- 妥善保存恢复码，不要只保存在同一台手机上。
- 不要把密码、验证码、身份证信息或 API 密钥提交到 GitHub。

## 十二、备份和恢复

GitHub 保存每次提交的历史记录，本地项目目录是另一份副本。

建议：

- 修改前先运行 `git status`，确认工作区状态。
- 每次只提交一类清晰的修改。
- 重要原始照片在其他硬盘或云盘中另存一份。
- 不要把 GitHub 当作唯一的照片备份工具。

查看最近的版本记录：

```powershell
git log --oneline -10
```

如果误删或误改了文件，不要立即运行 `git reset --hard` 等命令。先停止操作，确认需要保留的内容，再从 Git 历史或 Vercel 旧部署中恢复。

## 十三、常见问题

### 本地有内容，线上没有

依次检查：

1. 文件是否保存。
2. `git status` 是否显示改动。
3. 是否完成 `git commit`。
4. 是否完成 `git push origin main`。
5. Vercel 最新 Deployment 是否成功。

### Vercel 构建失败

1. 打开 Vercel 项目的 Deployments。
2. 进入失败的部署并查看 Logs。
3. 找到第一条真正的错误信息。
4. 在本地运行 `npm.cmd run check` 和 `npm.cmd run build` 复现。

### Markdown 页面不显示

检查：

- 文件是否放进正确文件夹。
- 文件扩展名是否为 `.md` 或 `.mdx`。
- 文件名是否以下划线开头；以下划线开头的文件不会发布。
- `title`、`pubDate` 和两组 `---` 是否正确。
- 是否设置了 `draft: true`。

### 照片不显示

检查：

- 图片是否确实放在 `src/content/photos/` 中。
- `data.json` 中的 `id` 是否与路径完全一致。
- `id` 是否错误地写入扩展名。
- JSON 中是否漏掉逗号或使用了中文标点。

## 十四、每次发布检查清单

- [ ] 内容和日期填写正确。
- [ ] 文件名使用英文、数字和连字符。
- [ ] 图片路径与 `id` 一致。
- [ ] 本地页面显示正常。
- [ ] `npm.cmd run check` 通过。
- [ ] `npm.cmd run build` 通过。
- [ ] `git status` 中只有准备发布的修改。
- [ ] 只暂存需要发布的文件。
- [ ] 完成 `git commit` 和 `git push origin main`。
- [ ] Vercel 部署成功。
- [ ] `https://gucheng.ink` 显示新内容。

## 十五、相关链接

- 网站：[https://gucheng.ink](https://gucheng.ink)
- GitHub 仓库：[https://github.com/wenkang-yang/gucheng-page](https://github.com/wenkang-yang/gucheng-page)
- Astro 内容集合文档：[https://docs.astro.build/en/guides/content-collections/](https://docs.astro.build/en/guides/content-collections/)
- Vercel Domains 文档：[https://vercel.com/docs/domains](https://vercel.com/docs/domains)
