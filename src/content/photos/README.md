# 如何添加照片

1. 把 JPG、PNG、WebP 或 AVIF 照片放进这个文件夹，也可以建立子文件夹分类。
2. 在 `data.json` 中按照展示顺序登记照片。

例如放入 `campus/spring.webp` 后：

```json
[
  {
    "id": "campus/spring",
    "desc": "春天的校园"
  }
]
```

`id` 不需要写扩展名；`desc` 是照片说明，也会帮助使用读屏软件的访客理解照片内容。
